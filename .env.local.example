# Supabase (Project Settings -> API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Stripe (Developers -> API keys)
STRIPE_SECRET_KEY=

# Stripe Price IDs — one per product in src/lib/shop.ts
# Create each Product in Stripe, copy the Price ID (starts with "price_")
STRIPE_PRICE_DESK_SET=
STRIPE_PRICE_EXTRA_CAMERA=
STRIPE_PRICE_EXTRA_SCREEN=
# Older name still works for the desk set:
# STRIPE_PRICE_CLASSROOM_KIT=
