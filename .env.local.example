# Supabase (Project Settings -> API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Stripe (Developers -> API keys)
STRIPE_SECRET_KEY=

# Stripe Price IDs — one per hardware kit in src/lib/hardwareKits.ts
# (Products -> your product -> Pricing -> copy the Price ID, starts with "price_")
STRIPE_PRICE_CLASSROOM_KIT=
STRIPE_PRICE_SCHOOL_BUNDLE=
